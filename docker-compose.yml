version: '3.7'
services:
    node:
        build: .
        restart: always
        ports:
          - 3000:3000
        # environment:
        #  - MYSQL_PASSWORD=password
        env_file:
          - .env
        # depends_on:
        #  - dbmysql
